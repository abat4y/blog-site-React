import { Link } from "react-router-dom";

const NotFound = () => {
    return ( <div>
        <h1>the is not found </h1>
        <Link to="/">go to home page</Link>
    </div> );
}
export default NotFound;